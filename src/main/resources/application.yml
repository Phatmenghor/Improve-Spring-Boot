spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneshop_development
    username: phoneshop_development
    password: Hour1819
  jpa:
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
